version: 2

sources:
  - name: raw_data_circle
    schema: raw_data_circle
    tables:
      - name: raw_cc_parcel
      - name: raw_cc_parcel_product


models:
  - name: stg_cc_parcel
    columns:
      - name: parcel_id
        tests:
          - not_null
          - unique
          
  - name: raw_cc_parcel_product
    columns: 
      - name: "(parcel_id || '-' || model_name)"
        tests:
          - not_null
          - unique
